---
description: Search the web via Ollama and print results inline
---

Use the `ollama_web_search` tool with the user's argument as the `query`. Use the default `max_results` (5) unless the query is broad. Print the returned results verbatim — do not summarize or omit any of them.